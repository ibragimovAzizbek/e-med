import 'package:flutter/material.dart';

class AddNewAppointmentPageDoctor extends StatelessWidget {
  const AddNewAppointmentPageDoctor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
